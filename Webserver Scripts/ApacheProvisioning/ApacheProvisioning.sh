
#!/bin/bash
#
# Processes to implement:
#
# Provision new site to pool (create config, create www dir, restart server, obtain SSL certs, restart server, notify user)
# Enable site from pool (symlink, restart server)
# Disable site from pool (rm symlink, restart server)
